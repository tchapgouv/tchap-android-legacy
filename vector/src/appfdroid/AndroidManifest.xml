<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="im.vector"
    android:installLocation="auto">

    <application
        android:name="im.vector.VectorApp"
        android:allowBackup="false"
        android:icon="@drawable/ic_tchap_red"
        android:label="@string/riot_app_name"
        android:largeHeap="true"
        android:logo="@drawable/ic_tchap_red"
        android:theme="@style/AppTheme"
        tools:replace="allowBackup,label">
        <provider
            android:name="im.vector.db.VectorContentProvider"
            android:authorities="fr.gouv.rie.tchap.provider"
            android:exported="true" />
    </application>
</manifest>
